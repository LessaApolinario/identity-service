services:
  identity-service-backend:
    build:
      context: .
      dockerfile: .docker/Dockerfile.main
      args:
        - NODE_VERSION=${NODE_VERSION:-24-alpine}
        - PORT=${PORT:-3000}
    container_name: identity-service-backend
    restart: always
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    depends_on:
      - identity-service-backend-database
    env_file:
      - .env
    networks:
      - identity-service-network
      - notes-bff-network

  identity-service-backend-database:
    image: postgres:17-alpine
    restart: always
    container_name: identity-service-backend-database
    ports:
      - "${POSTGRES_HOST_PORT:-7401}:5432"
    env_file:
      - .env
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - identity-service-network

volumes:
  pg_data:

networks:
  identity-service-network:
  notes-bff-network:
    external: true
